import React from 'react';
import { Provider } from 'react-redux';
import { mount, ReactWrapper } from 'enzyme';
import { afisha } from './hooks';

describe('There are events on afisha component', () => {
  it('Test some shit', () => {
    console.log('Its oke');
  });
});
